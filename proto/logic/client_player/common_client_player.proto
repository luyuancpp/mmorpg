syntax = "proto3";

option go_package = "pb/game";

import "common/tip.proto";
import "common/common.proto";
option cc_generic_services = true;

service ClientPlayerCommonService 
{
  rpc PushTipS2C (TipMessage) returns (TipMessage);
  rpc BeKick (TipMessage) returns (TipMessage);
}