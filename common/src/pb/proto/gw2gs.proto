syntax = "proto3";
package gsw2g;
import "common.proto";
option cc_generic_services = true;

message LogicRequest
{
 
}

message LogicResponse
{
	ClientError error = 1;
}

service Gw2gsService {
  rpc PlayerService (LogicRequest) returns (LogicResponse);
}
