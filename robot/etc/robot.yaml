# Robot load-test configuration
#
# Robot calls HTTP POST /api/assign-gate on the Gateway to get a gate address.

gateway_addr: "http://127.0.0.1:8081" # Gateway HTTP endpoint
zone_id: 1 # target zone (0 = auto-select recommended zone from /api/server-list)
robot_count: 2000
account_fmt: "robot_%04d"
password: "123456"
skill_ids: [] # empty = auto-read all skills from Skill.xlsx via table_dir; or set explicit IDs: [1, 2, 13]
table_dir: "../generated/tables" # path to generated/tables dir (relative to working dir or absolute)
action_interval: 3 # seconds between AI actions
report_interval: 5 # seconds between stats prints

# Mode: "stress" (default) = mass concurrent bots doing AI actions
#        "login-test"       = run login test suite (10 scenarios) then exit
mode: "stress"

# Auth type: "" or "password" (default), "satoken" (requires satoken_addr)
# auth_type: "satoken"
# satoken_addr: "http://127.0.0.1:18080"

# Behavior profile: "stress", "behavioral", "fighter", "explorer", "chatter"
# stress = combat-heavy with occasional scene switching (good for pressure tests)
# behavioral = mixed gameplay actions for realistic robot behavior / LLM training traces
# fighter = skill-heavy combat profile
# explorer = scene switching + movement heavy profile
# chatter = social/light activity profile
profile: "stress"

# Or define custom weights (overrides profile when set):
# custom_weights:
#   cast_skill: 50
#   move: 20
#   switch_scene: 20
#   idle: 5
#   chat: 5

# LLM-driven behavior (optional).
# Best for 1-10 robots for realistic player simulation. Not for mass load testing.
# Works with OpenAI, Ollama (http://localhost:11434/v1/chat/completions), vLLM, etc.
llm:
  enabled: false
  endpoint: "http://localhost:11434/v1/chat/completions"
  api_key: "" # optional, for OpenAI/Azure
  model: "qwen2" # model name
